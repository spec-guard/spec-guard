# Plans

Implementation plans paired with specs in [`../specs/`](../specs/). Plans decompose an approved
spec into small, verifiable increments and record the intended verification path.

In product repos initialized with `specguard init --scaffold`, this directory is created for
project-specific plans. In this repository, plans are added when a change needs more detail than
the spec and ADR already provide.
