---

## In GitHub Copilot

The loop is available as prompt files under `.github/prompts/` (`spec-orient`, `spec-write`,
`spec-verify`, `spec-sync`, `spec-status`). This governance also lives as always-on background
guidance in `.github/copilot-instructions.md`. Treat every non-trivial change as gated by the
loop above.
