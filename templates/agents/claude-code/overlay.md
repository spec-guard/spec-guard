---

## In Claude Code

Slash commands map to the loop: `/spec:orient`, `/spec:write`, `/spec:verify`, `/spec:sync`,
`/spec:status`. This skill is also injected automatically every session by the SessionStart
hook, and a `[SPEC-GUARD]` badge shows in the statusline while active. Turn it off with
`spec-guard off`.
