---

## In Gemini CLI

Installed as the `spec-guard` extension. The loop is available as custom commands
(`/spec:orient`, `/spec:write`, `/spec:verify`, `/spec:sync`, `/spec:status`) and the skill is
activated each session via the extension's hooks. Project conventions are also recorded in
`GEMINI.md`.
